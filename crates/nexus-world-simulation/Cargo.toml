[package]
name = "nexus-world-simulation"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Sandboxed world simulation for safe agent testing and scenario exploration"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
sha2 = "0.10"
thiserror = "1"
tracing = "0.1"
