[package]
name = "nexus-browser-agent"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Governed browser automation for Nexus OS agents via browser-use Python bridge."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1.10", features = ["v4"] }
thiserror = "2"

[lints]
workspace = true
